NEXT_PUBLIC_DEV_USER_ID=
# Optional: API origin for Next BFF (default http://localhost:8000)
API_BACKEND_URL=http://localhost:8000
# Must match API PROXY_SHARED_SECRET so X-Forwarded-Host is trusted
PROXY_SHARED_SECRET=local-dev-proxy-secret
