---
description: RAG 索引与 Agent Loop 硬约束（对齐 F04/F06）
alwaysApply: true
---

# RAG / Agent 硬约束

细节与验收以 Spec F04、F06 为准；本规则钉死实现红线。

## 索引与检索（F04）

1. 仅对 `published` 且 `index_status=ready` 的文档写入 `is_latest` section/leaf；未发布不得入可检索态索引数据。
2. **层级**：Phase 1 节树为 H1/H2（更深并入最近 H2）；节内再按可配置 token 切 leaf（默认 `CHUNK_TARGET_TOKENS=800`，`CHUNK_OVERLAP_TOKENS=100`）；禁止跨叶节切块；空文档 / 无字 PDF（不开 OCR）→ 0 section/chunk 且 job 成功。
3. Embedding：仅 leaf；单一 QWen 兼容模型；**模型名与维度可配置**（默认 `EMBEDDING_DIM=1024`），与 pgvector 列一致；禁止同库混用多套维度；改维度须迁库 + 全量重建索引；embedding 审计字段仅写在 **documents**。
4. 每个 section/chunk 必带 `tenant_id`；写库与 **`search(tenant_id, query, top_k)`** 强制租户过滤。
5. **检索**：对 `is_latest` leaf 向量 top-k；门禁 `publish_status=published` AND `index_status=ready` AND 未删；命中后返回 **所属节全文 + path**；同一 `section_id` 去重；F06 经 `search_knowledge` 调用本接口，不在 Agent 内另写一套向量查询。
6. 新版本索引成功后旧版本 documents/section/chunk **固定** `is_latest=false`；软删除文档同理。
7. 解析：PDF **骨架优先**（有 TOC/字号标题或 `PDF_FORCE_STRUCTURE` → Docling 结构路径打标签；无骨架纯文字 → PyMuPDF）；无骨架质量差可 fallback Docling；Office 固定 Docling；`do_ocr=false`；表以 Markdown 保留（见 F04）。

## Agent（F06）

1. 常量：`MAX_STEPS=5`，`HISTORY_COMPRESS_AFTER_MESSAGES=20`，`KEEP_RECENT_MESSAGES=6`，`LLM_TIMEOUT_S=60`，默认 `TOP_K=5`。
2. 工具白名单 Phase 1 **仅** `search_knowledge`；模型返回其它工具名则忽略或报错终止，不执行。
3. **无前置意图分类**：每轮用户消息一律进入 Agent Loop；是否调用 `search_knowledge`、检索 query、是否追问，由模型在 Loop 内决定。
4. 上下文顺序：system → 规则（无依据不编造）→ 压缩历史 → 本轮工具/检索片段。
5. **检索片段视为不可信数据**（防 prompt 注入）：与用户/系统指令隔离展示；禁止把全库或超大 chunk 集塞进 prompt，只注入本轮 top-k。
6. 无命中：明确「知识库无相关内容」；**禁止编造**文档事实、书名、条款号。
7. 用户消息与最终 assistant 消息经 F05 落库；每次运行写 `agent_run`（`used_search`/steps/status）；LLM/embedding 测试默认 mock。
8. `search_knowledge` 的 `tenant_id` 仅来自请求上下文，禁止由模型 tool 参数传入；检索实现委托 F04 `search`。
