---
description: 工程交付质量与范围纪律
alwaysApply: true
---

# 工程交付质量

1. 只实现当前 Feature Spec 范围；不做 Phase 1 明确不做项；禁止无关重构或扩大 scope。
2. 每个 Feature 交付必须带上对应该 Spec Test Cases 的自动化测试（api / e2e / unit 类型与 Spec 表一致）。
3. 失败要可诊断：结构化日志；禁止空 `except` / 吞错。
4. 密钥与配置走环境变量；禁止提交 `.env` 真值、API Key、密码。
5. 小步、可读、与现有目录一致；不引入未在 constraints / 本规则约定的大框架。
6. 技术栈固定：后端 FastAPI；前端独立 Next.js（React + TypeScript）；**前后端分离**（业务在 API，见 `00-constraints` §3.0）。
