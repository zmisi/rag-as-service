---
description: 全项目根本约束；不可被 Phase/Feature 绕过
alwaysApply: true
---

# 00 根本约束

本文件是**全项目**最基本规则（权威正文在本文件，Cursor 每次会话自动注入）。不可被任何 Phase / Feature Spec 绕过。冲突时以本文件为准。

## 1. 域名与路由

| 表面 | 用途 | Phase 1 | Phase 2 |
|------|------|---------|---------|
| `lxzxai.com` | 主站：注册、登录 | 是 | 是 |
| `{subdomain}.lxzxai.com` | 租户服务入口（RAG 聊天 / Portal） | 是 | 是（FAQ 推荐等增强） |
| `{subdomain}.lxzxai.com/admin` | 租户文档管理 | 是 | 是（文件夹树、预览） |
| `{subdomain}.lxzxai.com/api` | 对外 API 整合网关 | **否** | **是** |

- 不再使用 `lxtechai.com`。
- Host 解析到租户：`subdomain` → `tenant_id`；未知 subdomain → 404。
- 租户根路径 = 聊天入口（Portal）；`/admin` = 管理端；`/api` = 对外整合（见 Phase 2 F11）。

## 2. 多租户与子域

- 一个注册用户创建一个租户；数据与向量索引按 `tenant_id` 隔离。
- 租户 `tenant_name`（Host 二级域名；历史文案亦称 subdomain）：注册时由用户选定，系统校验格式与**全局唯一**（Phase 1 选定后不可改，见 F01）。
- `tenant_name` 规则：
  - 小写字母、数字、连字符 `-`
  - 长度 3–32
  - 不以 `-` 开头或结尾
  - 保留字不可用：`www`、`admin`、`api`、`app`、`mail`、`static`、`cdn`、`lxzxai`
- Host 解析：`{tenant_name}.lxzxai.com` → `tenants.tenant_name` → `tenant_id`；未知 → 404。
- 跨租户读写一律禁止（含向量检索）。

## 3. 技术栈

| 组件 | 选型 |
|------|------|
| 后端 | FastAPI（Python） |
| 前端 | 独立 Next.js（App Router）+ React + TypeScript；经反代与 API 同站对外 |
| 数据库 | PostgreSQL |
| 向量 | pgvector |
| LLM | QWen |

### 3.0 前后端分离（硬规则）

整体架构**必须**前后端分离，不以 Next 全栈替代 FastAPI：

1. **业务与数据权威在 `apps/api`（FastAPI）**：鉴权、租户、文档、会话、索引、RAG Agent、DB 访问均在此。
2. **`apps/web`（Next.js）仅 UI**：页面与交互；浏览器只经同源 **`/backend/*`** 调 API（反代或本地 rewrite）。
3. **禁止**在 Next Route Handler / Server Actions / RSC 中实现业务规则、直连数据库、调用 LLM/embedding（允许极薄的反代/BFF，不得承载用例逻辑）。
4. **禁止**前端写死后端内网 `host:port` 作为权威入口；**禁止**把 RAG/索引 worker 放进 `apps/web`。
5. 未来 Web 以外的客户端（如桌面壳）应复用同一 FastAPI，不得为单端再复制一套业务后端。

细节目录与反代见 [06-repo-layout.mdc](06-repo-layout.mdc)、[05-nextjs.mdc](05-nextjs.mdc)。

### 3.0a 品牌图标（硬规则）

- **唯一项目品牌图标**：[`apps/web/public/brand-cube.png`](../../apps/web/public/brand-cube.png)（teal/slate 等距方块；须为真透明 RGBA，禁止棋盘格伪透明）。
- **浏览器 Tab / favicon / apple-touch**：全站（含 `lxzxai.com` 登录注册、`{subdomain}.lxzxai.com` Portal、`/admin` 等）统一用该图标；实现上同步 `apps/web/app/icon.png`、`apps/web/app/apple-icon.png`，并由根 `app/layout.tsx` `metadata.icons` 引用 `/brand-cube.png`。
- Portal 空态 hero、后续营销/壳层 UI 的产品标也复用同一文件；**禁止**另引入第三方品牌 Logo 作为本产品主标。

## 3.1 Schema

- 业务表统一置于 PostgreSQL schema **`rag_service`**；禁止在 `public` 下建业务表。
- 迁移（Alembic）须 `CREATE SCHEMA IF NOT EXISTS rag_service`，对象名限定为 `rag_service.{table}`。
- 应用连接可设 `search_path=rag_service,public` 或 ORM 显式指定 schema。

## 3.2 数据库时间戳（全表强制）

所有业务表（含关联表、任务表、chunk 表等）**必须**包含以下两列：

| 列 | 类型 | Insert | Update |
|----|------|--------|--------|
| `create_at` | `timestamp` | 默认值为写入时刻（`DEFAULT now()`）；应用可不显式传入 | **禁止改写**（应用层不得 UPDATE 该列） |
| `update_at` | `timestamp` | 默认值为写入时刻（`DEFAULT now()`） | **必须**由 trigger 更新为本次写入时刻 |

实现要求：

1. 用 PostgreSQL **trigger**（共用函数 `rag_service.f_common_update_at()` + 每表 `BEFORE UPDATE`）维护 `update_at`；禁止仅靠应用层自觉赋值作为唯一手段。
2. `f_common_update_at()` 行为：`NEW.update_at := now()`；**不**修改 `create_at`。
3. 每表 trigger **命名**：`tr_{表名}_lmt`（例：`rag_service.users` → `tr_users_lmt`）。
4. Feature Spec「数据与边界」中可省略这两列的重复罗列；**未写不等于不存在**——建表时一律带上。
5. 迁移/schema 测试须能证明：任意表 insert 后两列非空且接近当前时间；update 后仅 `update_at` 前进、`create_at` 不变。

## 4. 认证

- Phase 1 **仅 Email** 注册与登录（会话 cookie）。
- 微信登录：**不做验收**；标 **Phase 3**（见 [01-phase-list.md](../../docs/specs/01-phase-list.md)）。
- 会话：cookie `Domain=.lxzxai.com`；主站与 `{subdomain}.lxzxai.com`（含 `/admin`）同一会话族。
- Phase 2 对外 API：租户 **API Key**（Bearer）；Embed Widget：公开 **site key** + Origin 白名单（见 F11/F12）。与成员 cookie 会话分离。

## 5. 文档与索引

- 文档状态机：`draft` → `review` → `published`（细节见 F03）。
- **仅 `published` 文档进入索引**（F04）；未发布文档不可被 RAG 检索（F06）。
- `review` 是可测状态，**不做 SOP 内容强制校验**（SOP 门禁属 **Phase 3**）。
- Phase 1 类型：`.txt` / `.md` / `.pdf`（不支持旧版 `.doc` / `.ppt` / `.xls`）。
- Phase 2 Office OOXML（`.docx` / `.xlsx` / `.pptx`）上传与索引：见 F08。
- 版本从整数 `1` 起（展示 `v1`）；见 F03/F07。

## 6. Agent（Phase 1）

- 租户入口当前能力：RAG Agent（检索 + 多轮对话）。
- LLM 调用使用 QWen。
- Agent Loop（无前置意图分类）、上下文组装、记忆压缩、工具调用：见 F06。

## 7. Phase 边界（摘要）

### Phase 1 明确不做（仍成立）

- 微信登录验收（Phase 3）
- SOP 必须验证成功才能上传/发布（Phase 3）
- 跨租户共享知识库

### Phase 2 范围（见 [phase2/](../../docs/specs/phase2/)）

- Office OOXML（`.docx` / `.xlsx` / `.pptx`）上传与索引
- Admin 文件夹树、文档预览
- `{subdomain}.lxzxai.com/api` + API Key；Embed Widget
- Portal FAQ 推荐（热度 / 换一批）；Portal 壳与 New task 延迟会话（F14）

### Phase 3 预留

- 微信登录、SOP 强制验证门禁；OCR / 任意深标题树等增强

## 8. Feature 状态（实现门禁）

每个 Feature Spec 必须标注状态，取值仅限：

| 状态 | 含义 |
|------|------|
| `draft` | 起草中 |
| `review` | 提请 team review |
| `approved` | 已批准，允许实现 |
| `done` | Test Cases 全部通过 |

硬规则：

1. **未 `approved`（及未到 `done`）的 Feature 不得进入实现与验收编码。**
2. `draft` / `review` 仅允许改 Spec；发现实现中的代码针对未批准 Feature，视为违规。
3. `approved` → 实现 → Test Cases 全过 → 改为 `done`。
4. Feature List 须有 Status 列，与各 Feature 文首状态一致。

## 9. 验收

- Feature 是最小交付单位。
- **唯一验收标准**：该 Feature Spec 中的 Test Cases 全部通过。
- 无对应 Test Case 的行为，不算该 Phase 范围。
