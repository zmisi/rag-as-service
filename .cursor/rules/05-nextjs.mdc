---
description: 独立 Next.js（React）前端约定
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Next.js 约定

1. 独立前端：Next.js App Router + TypeScript + React；调用 FastAPI，不把业务/RAG 逻辑搬进 Next Route Handler（除薄反代/BFF 外）。
2. 服务端依据 Host / cookie 解析租户与登录态；租户页不以客户端「猜 subdomain」为权威。
3. 主站（注册/登录）与 `{subdomain}.lxzxai.com` 租户站（聊天、`/admin`）路由清晰拆分；未登录访问 `/admin` 按 F02 重定向 `lxzxai.com/login`。
4. 浏览器只请求同源反代路径（如 `/backend/...`），不写死后端内网 URL。
5. 聊天可流式展示，但最终 assistant 消息须可测落库（F06）；UI 不做 Spec 外产品功能。
6. 避免无 Spec 要求的营销落地页、多余 dashboard 卡片或装饰性堆砌。
